# Configurations

- `learning_minimal.yaml`: local smoke test and CPU dry-run.
- `maskrcnn_resnet50_fpn.yaml`: a local full-data baseline without pretrained weights.
- `reference_maskrcnn.yaml`: the 20 epoch Kaggle GPU reference run with COCO weights.

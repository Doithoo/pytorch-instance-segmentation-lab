# Configuration Reference

`run.name` identifies an artifact directory. `data.factory` is optional `module:callable`; otherwise `data.provider=pennfudan` is used. `model.params` forwards supported torchvision constructor options. `training.best_metric` is fixed to `mask_map`. `device` is `auto`, `cpu`, `cuda`, or `mps`.

# Experiments

Use a new `run.name` per experiment. Compare `metrics.csv`, resolved `config.yaml`, manifest hashes, and evaluation JSON together. Do not compare metrics produced from different test splits or thresholds. Keep checkpoints outside Git and record their SHA-256 for published runs.

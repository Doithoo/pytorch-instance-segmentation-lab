# 训练记录状态

状态：`COMPLETE_PROTOCOL_V2`

Kaggle kernel version 2 已在 Tesla T4 上完成全部 20 个 epoch。第 10 轮以验证 `mask_map=0.7666944265365601` 选出；随后固定的按来源分层 test split 得到标准置信度排序 `mask_map=0.7560926079750061` 和 `bbox_map=0.8464389443397522`。完整 provenance 位于 `run.yaml` 和 `kaggle-run-summary.json`。协议 v1 历史保留在 `legacy-v1/`。
